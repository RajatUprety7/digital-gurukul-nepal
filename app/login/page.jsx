"use client";
import { useState } from "react";
export default function Login(){const[form,setForm]=useState({email:"",password:""});const[message,setMessage]=useState("");async function login(e){e.preventDefault();setMessage('Logging in...');const res=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const data=await res.json();if(!data.success)return setMessage(data.message||'Login failed');if(data.user.role==='admin')window.location.href='/dashboard/admin';else if(data.user.role==='instructor')window.location.href='/dashboard/instructor';else window.location.href='/dashboard/student'}return <main className="login"><form className="form" onSubmit={login}><div className="logo">DG</div><h1>Login to Integrated Platform</h1>{message&&<div className={message.includes('Invalid')?'message error':'message'}>{message}</div>}<label>Email<input value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>Password<input type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label><button>Login</button><small>Admin: admin@digitalgurukulnepal.com / Admin@123<br/>Instructor: instructor@digitalgurukulnepal.com / Instructor@123<br/>Student: student@digitalgurukulnepal.com / Student@123</small></form></main>}
